# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SPAppStoreController' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SPAppStoreController
  pod 'Cards'
  
  # pods for reddit download
  pod 'SwiftyJSON', '~> 4.0'
  pod 'SDWebImage', '~> 4.0'
  pod 'SDWebImage/GIF'
  pod 'IDMPhotoBrowser'
  pod 'UIScrollView-InfiniteScroll', '~> 1.0.0'
  

  target 'SPAppStoreControllerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SPAppStoreControllerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
